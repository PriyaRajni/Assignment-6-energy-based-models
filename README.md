Normalizing Flow (NF) models are a class of generative models that learn complex data distributions by transforming a simple probability distribution (like a Gaussian)
into a complex one using a sequence of invertible, differentiable transformations.
